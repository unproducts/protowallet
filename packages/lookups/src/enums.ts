export enum Currency {
  INR,
  USD,
}

export enum RecordType {
  Expense,
  Income,
  Transfer,
}

export enum RecordDirection {
  Right,
  Left,
}

export enum EndRecurrenceBy {
  Count,
  EndDate,
  NeverEnd,
}
