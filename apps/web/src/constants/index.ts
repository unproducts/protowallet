// Currently keeping all constants at one place
// In future if they increase we can keep them in different files 

export const recordTypes = ['Expense', 'Income', 'Transfer'];
export const endRecurrenceBy = ['count', 'date'];